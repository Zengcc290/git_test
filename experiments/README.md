# 实验区

一次实验一个子目录，建议包含 `README.md`、配置、运行入口和 `outputs/`。实验结果应记录数据集版本、模型版本、参数、指标和结论，保证可复现。

- `notebooks/`：探索性分析和教学 Notebook
- `benchmarks/`：检索、生成和端到端基准
- `ablations/`：chunk、Embedding、Top-K、Reranker 等消融实验
