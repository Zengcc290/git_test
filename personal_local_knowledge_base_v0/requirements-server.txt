# Install these on the GPU server, not in the local knowledge-base venv.
# Pick a torch build compatible with the server's NVIDIA driver/CUDA runtime.
torch>=2.1,<2.6
transformers>=4.51,<5.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
